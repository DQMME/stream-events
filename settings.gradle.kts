rootProject.name = "stream-events"

pluginManagement {
    repositories {
        gradlePluginPortal()
        // PaperMC Repo
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}
